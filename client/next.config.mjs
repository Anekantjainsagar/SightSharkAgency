/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.pinimg.com",
      "storage.googleapis.com",
      "lookerstudio.google.com",
    ],
  },
};

export default nextConfig;
